export const GRID_SIZE = 20;
export const SNAP_THRESHOLD = 10;
export const DEFAULT_WALL_THICKNESS = 10;
export const DEFAULT_WALL_LENGTH = 100;

export const INITIAL_VIEW = {
  x: 0,
  y: 0,
  zoom: 1
};
